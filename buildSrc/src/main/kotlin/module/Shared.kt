package module

object Shared {
    const val group = ":shared"

    const val domain = "$group:domain"
    const val data = "$group:domain"
    const val local = "$group:domain"
    const val network = "$group:domain"
}
